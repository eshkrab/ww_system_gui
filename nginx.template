server {
  listen 80;
  root /usr/share/nginx/html;

  location / {
    if ($request_method = GET) {
        rewrite ^/(.*)$ /$1?serverIP=$server_ip&serverPort=$server_port;
    }

    try_files $uri$args $uri$args/ /index.html$args =404;
  }
}
